rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Public read access for project images
    match /projects/{imageId} {
      allow read: if true;
      // Only admin via Firebase Console can upload/delete
      allow write: if false;
    }
  }
}
